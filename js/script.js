const list = [
    'Latte',
    'Cacao',
    'Insalata',
    'Carote',
    'Pomodori',
    'Piatti pronti'
];

console.log (list);

for (let i = 0; i < list.length; i++) {
    let elementList = list[i];
    console.log (elementList);
}